# 六经注我 · 原生小程序

## 当前执行版本 · 2026-09-23

分叉经文链已获实施授权。最新确认优先于下方历史记录和早期提案：**保留全屏主视觉与无限向上环流；流动时只见经文，解释和可点字词仅在停驻时随原有阅读层展开。** 不为短解另留底部区域，不在内容批次末尾停止经轮。

当前加入链、入口、父链位置保存、原生短解组件、预取/取消、`/api/flow` 服务及动态字体分片。服务端已在本机调用真实 DS 验证；未发布服务。完整模块、接口和验收结果见 [实施记录](BRANCHING-IMPLEMENTATION.md) 和 [运行说明](../README.md)。下面各日期的 mock/字体规模/API 状态保留为当时的历史基线，不代表当前实现边界。

## 已确认目标

用户于本次任务明确授权：结合所提供 React/Three 参考的黑底、玻璃透射与色散，以及 Simon Rogers 的持续上升运动，原生实现微信小程序。后续视觉反馈将形体明确为**竖直转经筒**：五层水平经文环绕同一根竖轴，中间清晰，上下逐层渐弱、渐隐、渐模糊。初始一念之后持续流动，不逐轮要求输入。本轮使用 mock 内容，DeepSeek V4.1 Flash 留待后续接入。

## 设计与模块

- `pages/flow`：单场景中的流动、停驻解读与原文展开；一次输入、拖动回看；原生 WXML/WXSS，没有网页嵌套。
- `flow/scene`：竖直封闭圆柱、水平环绕文字、五层空间焦点；每段经文拆为短句，每圈只呈现一句。
- `flow/renderer`：原生 Canvas WebGL 1；白色字形图集、两个带深度缓冲的离屏场景，分别计算背面和正面的体积折射；不同波长使用不同折射率。前景文字保持白色，色散由玻璃产生。固定竖轴、整体同向旋转并上升；中段减弱折射叠影，保留文字可读性。绘制频率上限约 30 fps，DPR 上限 2。v5：行改为屏幕空间 ribbon 排布——正弦剖面让中段行距均匀、向上下边缘压缩并铺满全屏，行绕自身切线倾倒（中心直立、边缘近平躺且始终朝向读者），离焦行按真实焦距差产生景深模糊与色差；边缘淡出在片元级计算（顶点级插值会把只有两个纵向顶点的玻璃幕墙整体清零）。
- `flow/timeline`：独立时钟；按时间推进，触摸暂停推进、隐藏停止、恢复不快进。
- `flow/provider`：mock provider；`open(seed)` 返回 `{kind, seed, seedOrigin, journey, frames, cursor}`，展示层不依赖模型、凭证或网络协议。
- `content/passages`：从项目本地语料选取并保留 sourceId/fullText；三个主题各八段，解读为明确的人工示例。mock 按整组自然回环，不声称无限生成。

视觉：黑色空间、白色宋体经文、玻璃折射中的青/蓝/暖红色散。流动时不显示标题、品牌副标题、念头摘要、桥接语或解读。首次提示「轻点停驻，读懂这一句」自动消失。轻点后当前短句用 420 ms 归中，其他经句退暗，下方展示完整经句、句意和此刻解读，底部出现「一念 / 原文 / 继续」。

## 黑场材质 · 2026-09-22

用户批准增加极淡渐晕、静态墨颗粒与随流动变化的呼吸。叠加后的背景上限约为白色亮度的 3%，保持经文为唯一明亮主体：

- `flow/atmosphere`：中心基准亮度 2.1%，呼吸幅度 ±0.4%；64×64 单通道噪点纹理仅 4 KiB，静态颗粒峰峰差为 1%。径向场平滑退至画布四边的纯黑，与原生页面接合。
- `flow/renderer`：只在最终画面加入背景，玻璃内外使用相同的屏幕坐标场；不将颗粒送入折射缓冲，不改变文字、字号、几何和色散参数。新增一次背景绘制，不增加离屏缓冲或独立动画循环；销毁时释放纹理与程序。
- `flow/timeline`：亮度相位随实际流动或拖动位移推进，正常流速下一轮约 18–35 秒；快速惯性滑动也限制在最快约 18 秒一轮。停驻/后台冻结原亮度，恢复从同一相位继续，程序归中不触发额外呼吸。
- 页面读取 WXSS 的 `prefers-reduced-motion` 结果，传入 renderer；开启时仅保留静态渐晕和颗粒。读取不可用时也保持静态。返回前台重新读取偏好；这只控制新背景呼吸，不声称已经为原有经轮自动流动实现系统减动适配。
- 停驻仍使用同帧截图承接，背景一并冻结，原文展开时与同一句经文一起移动。

## 连续阅读迭代 · 2026-09-22

用户批准在现有视觉基础上迭代连续性、补充解释，并在迭代前后推送版本。页面状态为 flow → settling → reading → resuming；原文是 reading 的展开层，不再切到另一套静读布局。

- 当前短句是共同的视觉锚点。归中后通过原生 `wx.canvasToTempFilePath` 保存同一帧，等原生 image 解码后才隐藏 WebGL；由此避开原生画布覆盖 WXML 的层级限制。解读淡入时，锚点的像素位置、尺寸和玻璃效果保留。阅读时停止 GPU 绘制。
- 原文展开：同一帧与下方阅读区一起上移最多 160 px，340 ms 完成；下方露出出处、原文及所选经句的高亮。收起反向执行，不更换经句。正常阅读与图形故障静读均按完整段落导航。
- 返回流动：解读 220 ms 退场，原文如已展开先回位；恢复同一画布，再用 240 ms 恢复周围经句，自动流速从零渐增。暂停和拖动在状态切换时重置速度，不依赖已经停止的绘制循环。
- 一念抽屉先挂载再进入，320 ms 后聚焦；关闭有 240 ms 退出期。新主题的旧画面保留到新画面准备完成，以两张原生画面淡接后恢复实时流动。
- `meaning` 与 `reflection` 由 mock provider 按完整段落返回，原文独立；流动短句带有 `passageId / passageQuote / passageLines / lineIndex`，避免停在后半句时丢失语境。
- 所有异步截图、图片解码、动画完成及键盘延迟受动作编号约束；新操作、后台、resize、卸载取消旧回调。迟到的内容请求仍按请求编号校验，并能在回前台后完成。截图承接失败进入可阅读的原生降级状态。
- WXSS 尊重 `prefers-reduced-motion`；停驻是手动静读入口。WebGL 自动流动尚未读取系统的减少动态效果偏好，不声称已验证该平台能力。

实际参考来源：用户提供的 `src/ring.tsx`（圆柱、白字、MeshTransmissionMaterial，thickness=2/backsideThickness=5），[Simon Rogers 原站](https://www.simonrogers.info/) 的逐行 rotateY + translateY 动画，以及用户新指定的转经筒结构；结构核对参考 [Met 藏品](https://www.metmuseum.org/art/collection/search/32640)。最终按用户要求取消斜向/螺旋升角，采用水平文字带和固定竖轴。

本轮在当前仓库增加独立 `miniprogram/`，不需要分支或工作树。使用微信开发者工具原生模拟器验证。用户已指定测试 AppID `wx5f0a423b84cafbef`；当前阶段不上传、不发布、不请求 LLM。

## 验收

2026-09-22 字体迭代：用户明确选择「中间一行大、上下几行小」。缩放跟随每一行距中心的连续距离，上下对称，不按行索引跳变；中间基准为原字号的 1.12 倍，向远端收至 0.62 倍，保留原有位置、旋转、景深与色散。按实际手机相机距离收紧过长经句的字距，中心字形维持同一字号。

用户随后指定复用相邻 CoScroll 的字体。已核对其 `src/app/page.tsx` 的 `DEFAULT_FONT_FAMILY` 和 `@font-face`，实际使用 `public/fonts/润植家康熙字典美化体.ttf`。页面文字、输入和字形图集统一用该字体的随包子集，原字体缺少的五个古字以单独的 Noto Serif SC 子集补齐，不改写原文。使用独立的 webview/native 作用域注册；绘制前等待注册完成，失败回退系统衬线体，避免未加载字体时先画出黑体。未来动态内容需要更新子集或加载完整/分片字体；不新增网络依赖。

用户随后反馈 iPhone 流动段仍是黑体、其他文字正确。画布路径现改为运行时读取页面使用的同一份字体文件：`flow/glyph-outline.js` 通过随包的 OpenType.js 查找 cmap、解析曲线并缓存，按字体进宽和基线绘入现有图集。不再导出或维护当前 91 个用字的独立轮廓表，也不依赖画布的 `fillText` 与 native 字体匹配；保持 `paintAtlas(canvas, frames, dpr)` 接口、纹理 UV、字号缩放及色散结构。页面继续使用正常工作的 webview 字体注册。

`createGlyphSource(sources)` 接受 base64、ArrayBuffer 或字节视图，可直接解析覆盖更多字符的 WOFF/TTF/OTF，不需要按新经文修改轮廓代码。当前资源仍为 665 字离线子集，未来无限内容需要完整/分片字体及加载逻辑；本次不把该资源边界误写成完整字库已经接入。字体未收录的字显式进入现有原生阅读降级，不接受静默回退黑体。验收包含禁用 Canvas 字体调用、新经文用字、补字、字体文件替换、缓存复用、DPR、原生截图及 iPhone 复验；真机验证仍待完成。

1. 原生小程序直接编译运行；无 web-view 和 React 运行时依赖。
2. 冷启动自动进入示例流；可输入一念并切换主题；之后无需反复输入。
3. 画面为同轴的五层经文，只显示经典短句；中间清晰，上下对称衰减；中心短句水平对齐。解读、出处和原文位于按需阅读层。
4. 点击停驻并居中/继续，拖动回看，查看原文并关闭后保持原阅读进度；后台不继续推进；整个 mock 周期首尾连续。
5. mock 跨主题有连续语义，经典出处可追溯；不把示例解读当作原文。
6. WebGL 不可用时保留静读与换段能力；故障可重试；静读可由用户主动选择。
7. 测试覆盖时间步进、拖动/回环、生命周期、内容真实性与 provider 边界；记录模拟器视觉与交互结果。

## 连续性与阅读迭代 · 46f538d 之后

用户批准按评审顺序实施。保留 46f538d 的共享字体解析、黑场材质和玻璃色散；完整在线字库与 DeepSeek 仍属后续接入。本轮模块与验收如下：

- `scene/renderer/timeline`：共享归中相位；各水平文字环的转角按其经过焦点的距离推进，确保前向经句不会在交接时全部跑到左右边缘。按用户随后确认的方向，相邻行一左一右交错慢转，五档角速度交替，仍绕同一竖轴持续上升。速度归属于经过画面的行，不随中心索引切换而跳变；跨内容循环也保留方向。统一投影计算供行命中使用；仅中心和邻近可读行可选，空白维持停驻/继续。停驻时所选行沿原运动平滑归中转正。相机与半径对宽屏保持正距离。
- `atlas`：按本批不同字符数选择图集大小，并遵守设备纹理尺寸上限；取消固定 100 字容量。字体解析和缓存继续复用，重复内容不逐帧绘字。超过设备容量或字体缺字仍明确降级，不冒充完整在线字库。
- `build-font/glyph-outline`：除缺字外，也检测有编码但没有笔画的空字形。将康熙字体的数字、拉丁字母和部分标点交给现有 Noto 补字，使序号、日期和间隔符正常显示。随包子集随本轮文案扩为 695 字；此前 665 字是 46f538d 的资源边界。
- `pages/flow`：手指按下仅冻结，超过拖动阈值才收起阅读；保持原画面交接和异步动作取消。短读呈现必要上下文、原义和示例联系；原文定位出处，分别记住两层滚动位置。当前一念只在停驻显示，区分用户输入与默认示例，修改保留原话。
- `provider/build-content`：新增四组明确的人工“一念—经典—联系”样本，修正知止的泛化解释。任意输入仍是主题 mock，标签继续标示示例，不伪装模型理解。
- `notes`：可选“留一句”在本机保存用户自己的文字、当时一念及出处；支持回看、删除与撤销，不自动生成感悟，不增加流动层常驻入口、不强制旅程收束。
- 减少动态效果：在 46f538d 静态背景分支之上，检测到系统偏好时直接停驻并取消归中动画；用户可明确选择继续。系统偏好在 iPhone 上的实际映射仍需真机复验。
- 验收覆盖完整交接相位、短长句和宽窄屏投影、选句与拖动取消、原文定位/返回、笔记存取失败和删除撤销，以及原生模拟器截图。提高说明与按钮字号，修复禁用按钮实际样式。正式验收截图归 `docs/shots`，临时 `.shots` 不进入版本或小程序包。
- 交错运动验收：连续时间片中相邻行角速度反向且幅值不同；奇数/偶数行数回环、拖动和归中不改变同一行的速度身份；停驻/后台冻结，继续沿原相位恢复。命中区域与 WebGL 使用同一组行速度和投影规则。

## 后续 DeepSeek

2026-09-22 核对官方文档 https://api-docs.deepseek.com/zh-cn/ ：V4.1 Flash 使用模型名 `deepseek-flash`，OpenAI 兼容地址 `https://api.deepseek.com/chat/completions`。

后续通过自己的服务端/云函数转发，服务端保存 `DEEPSEEK_API_KEY` 和模型配置。小程序只请求经文帧；服务端检索已验证原文，让模型生成 reflection/bridge，返回经过校验的结构化数据。切换 provider 时仍须处理缓存、预取、取消、重试、费用边界与内容审核；本轮不伪造线上接入。
