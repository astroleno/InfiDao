# 原生小程序验证 · 2026-09-22

## iPhone 流动字体回退修复

用户在 iPhone 实测发现流动经文仍为黑体，其他阅读文字已正确。此前模拟器里的 native 字体注册成功不构成真机字体正确的证据。

- 流动字形改为从同一康熙字体导出的闭合笔画轮廓，随包包含当前全部 91 个流动用字，数据 277,189 bytes。原来的图集、UV、WebGL 色散与逐行缩放保持不变；取消 Canvas 的字体注册和 `fillText` 依赖。
- `npm run check` 和 31 项测试通过；新增测试使字体名称访问与 `fillText` 调用直接抛错，三个 mock 路径在 DPR 1 / 2 下仍全部绘制成功；字形轮廓闭合、图集边界和缺字显式处理均通过。
- 原生模拟器将真实图集的 `fillText` 替换为抛错函数后重建图集，调用次数为 0，首字图块重建前后像素校验值均为 959413229，GL error=0。此处只比较首字图块，未将其当作整屏像素对比。
- 原生状态为 `glyphMode=bundled-outlines`；停驻后 `snapshotReady=true`，继续后恢复流动。视觉记录：[轮廓字体流动场景](shots/font-outlines-flow.jpg)。

本轮未连接用户的 iPhone，仍需重新编译手机预览后复验。开发者工具截图不能替代真机结果。

## 中心字号与 CoScroll 字体

- 核对相邻 CoScroll 主页面的 `DEFAULT_FONT_FAMILY = 'RunZhiJiaKangXiZidian'`，复用其实际「润植家康熙字典美化体」字体文件；保留来源与版权元数据。当前 665 个用字中，660 个来自主字体，缺少的五个古字由独立 Noto Serif SC 子集补齐。原文字符串保持不变，字体本身含繁体风格字形。
- WOFF 子集共 219,632 bytes，内嵌数据共 292,844 bytes，无网络字体请求。模拟器内 `webview` 和 `native` 都报告 `loaded`，字形图集使用 `128px RunZhiJiaKangXiZidian`。
- 中心行放大至原字号 1.12 倍，上下按连续距离向 0.62 倍收缩；保留景深、色散和流动轨迹。320 / 390 / 430 宽度下，放大后的 4–10 字中心句经投影计算均完整落在屏幕内。
- JavaScript / JSON / 页面结构检查、29 项测试、原生 WXSS 编译通过。新增覆盖内置字体字形范围、双作用域注册、缺字补充、注册失败回退和放大后边界。
- 原生模拟器确认新字体和字号层级，停驻后仍由同帧图像承接；点击继续恢复流动。截图：[流动层级](shots/typography-flow.jpg)、[停驻解读](shots/typography-reading.jpg)。流动层级截图在固定进度截取，便于比较各行大小。

仍需真机确认不同系统的字体注册、输入中的未收录字和渲染清晰度；当前字库覆盖 mock 与界面，后续动态内容需更新字形范围或接入完整/分片字体。自动化获取 computedStyle 遇到工具序列化错误，字体确认依据注册结果、画布字体属性与原生截图。

## 连续阅读迭代

- `npm run check` 通过；25 项 Node 测试通过。新增覆盖真实停循环后的渐速、同帧图片解码交接、迟到截图取消、原文退出、快速关闭输入的焦点取消、后台中断、迟到 provider、卸载、原生 touchend 与重复 tap、完整段落导航、图形故障阅读和 renderer 动画回调取消。
- 更新后的 WXML / WXSS 独立编译通过，随后完整编译运行。
- iPhone 12/13 (Pro) 原生模拟器确认：中间经句保持位置与大小，其他经句退暗；停驻后显示完整经句、句意与示例解读。
- 原文与锚点一起上移，继续后回到同一句，并恢复实时流动；GL error=0。输入新的起步念头后进入 act 路径，恢复流动。
- 使用原生窗口画布点击验证停驻；原生元素 touchstart/move/end 从阅读图片拖动，行索引由 8 移到 10 后重新停稳并显示对应解读。
- 运行时模拟在恢复过程中进入后台：位置 5355.52 保持不变；onShow 后恢复到 flow，snapshotReady=false，GL error=0。
- 注入图形错误后保留完整经句与句意；下一段从《孟子》转入《论语》对应解读；点击恢复流动后重新建立 WebGL，GL error=0。
- 独立编译诊断后曾出现开发者工具缓存缺失模块错误，清理本项目编译缓存并完整编译后恢复；另遇工具连接频率限制，后续已恢复。未更换测试号或修改全局配置。

本轮模拟器截图：[停驻与解读](shots/continuity-reading.jpg)、[展开原文](shots/continuity-source.jpg)。两张截图保留同一句经文，展示锚点上移和阅读内容展开。

真机 GPU、振动触感、系统键盘与系统减少动态效果偏好仍未实测；截图和生命周期模拟不能替代这些检查。内容保持 mock，未接入 DeepSeek，也未上传小程序。

以下记录保留前一版基线的验证历史。

环境：微信开发者工具 2.02.2608060，基础库 3.17.3，iPhone 12/13 (Pro) 模拟器；测试 AppID `wx5f0a423b84cafbef`。所有应用画面来自原生小程序模拟器，无网页替代。

## 通过

- WXML、WXSS 独立编译；JavaScript 语法、JSON 和页面资源检查。
- 9 项自动测试：时钟与帧率、暂停/后台、拖动/回环、长帧保护、mock 主题路径、原文出处、竖直圆柱几何、上下对称焦点、画面仅含经典短句。
- 原生画面五层水平文字同轴排列；中心清晰，上下逐层变暗和模糊。首屏无标题、解读、出处、念头摘要等常驻内容。
- 轻点停驻，420 ms 归中；显示「一念 / 原文 / 继续」；从停驻状态继续。
- 原生 touchstart/move/end 拖动回看；手指按下停止自动推进，拖动后恢复流动。
- 在原生 textarea 输入「我想开始，却总在犹豫」，提交后选中 act 路径，自动流动，窗口关闭，GL error=0。
- 原文抽屉按需出现；完整原文、出处、示例解读分别展示；关闭后回到原进度。
- 真实模拟器点击「静静读一会儿」、下一段经文、继续；确认短句与序号变化，并恢复 WebGL 流动。
- 运行时调用 onHide/onShow 验证后台处理：隐藏时位置不变、绘制停止；恢复从原位置小步推进。
- 注入渲染异常后进入原生静读，可继续换段；点击继续后成功重建 WebGL，故障状态清除。
- 完整 mock 周期首尾图像比较：画面主体 586×975 像素，通道平均差 0.000115/255，最大差 2/255；无可见循环跳变。比较排除了手机状态栏和底部控件。

本地验证截图位于仓库忽略目录 `.codex-screens/`：`wheel-final.png`、`wheel-aligned.png`、`wheel-loop.png`。最终模拟器恢复自动流动。

## 边界

- 未做手机真机 GPU、帧率、功耗和字体一致性测试。30 fps、DPR≤2 是实现预算，不是实机测量结果。
- 320/390/430 宽度下的中轴和圆柱边界通过几何测试；原生视觉检查使用上列模拟器尺寸。
- onHide/onShow 是在原生运行时调用生命周期处理函数验证，未替代真机切换后台测试。
- mock 按已整理的路径循环，不是模型无限生成。DeepSeek 尚未接入；未上传、发布或提交代码。
- 一次将延迟和像素读取放入同一异步自动化调用的检查超时；已拆为独立生命周期调用及模拟器截图对比完成验证。
