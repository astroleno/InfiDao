# Legacy scripts

These scripts belong to the retired database/model implementation and are not
part of the Reboot MVP. They are not run by CI or by the release path, and are
retained only as historical references.

Do not restore npm command entries for these files. The supported release path
uses local JSON/JSONL artifacts and the npm commands documented in `README.md`.
